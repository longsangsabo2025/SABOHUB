name: flutter_sabohub
description: 'SABOHUB - Nền tảng vận hành đa ngành nghề'
publish_to: 'none'
version: 1.8.3+26

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
    
  flutter_localizations:
    sdk: flutter

  # State Management - Riverpod 3.x 
  flutter_riverpod: ^3.0.3

  # Navigation
  go_router: ^14.8.0

  # Network
  http: ^1.2.2
  connectivity_plus: ^6.1.3  # For offline detection

  # Location Services - Updated to latest (2025)
  geolocator: ^14.0.2
  geolocator_android: ^5.0.2  # Phải phù hợp với geolocator 14.x
  geolocator_apple: ^2.3.10
  permission_handler: ^12.0.1
  geocoding: ^3.0.0
  
  # Background Geolocation - Sử dụng geolocator với foreground service
  # flutter_background_geolocation: Removed - license required for v5
  
  # Maps - Free OpenStreetMap solution
  flutter_map: ^8.2.2
  latlong2: ^0.9.1


  # Supabase
  supabase_flutter: ^2.10.3
  flutter_dotenv: ^6.0.0

  # Local Storage
  sqflite: ^2.3.0

  # Error Tracking
  sentry_flutter: ^8.12.0

  # UI Components
  flutter_svg: ^2.2.3
  cached_network_image: ^3.3.0
  google_fonts: ^6.1.0
  shimmer: ^3.0.0

  # Utilities
  intl: ^0.20.2
  equatable: ^2.0.5
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.2
  uuid: ^4.5.1
  
  # AI Features - Updated flutter_markdown
  file_picker: ^10.3.3
  markdown_widget: 2.3.2+6
  
  # Google Drive Integration
  googleapis: ^13.2.0
  google_sign_in: ^6.3.0
  extension_google_sign_in_as_googleapis_auth: ^2.0.13
  mime: ^2.0.0
  
  # Apple Sign In
  sign_in_with_apple: ^6.1.3
  crypto: ^3.0.3
  
  # Excel file parsing
  excel: ^4.0.6
  
  # Charts
  fl_chart: ^0.70.1
  image_picker: ^1.2.1
  
  # PDF & Printing
  pdf: ^3.11.2
  printing: ^5.13.4
  
  # Vietnam Address Picker
  dvhcvn: 2.1.20250301
  multiavatar: ^0.1.5
  confetti: ^0.8.0
  firebase_messaging: ^16.1.2
  firebase_core: ^4.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
  
  assets:
    - .env
    - .env.local
    - assets/images/
